# ============================================
# Esports Tracker - Production Environment
# ============================================
# Copy this file to .env and fill in the values

# ==========================================
# DOMAIN CONFIGURATION
# ==========================================
DOMAIN=esports-tracker.com
ACME_EMAIL=admin@esports-tracker.com

# ==========================================
# DATABASE (PostgreSQL)
# ==========================================
DB_HOST=postgres
DB_PORT=5432
DB_USER=esports
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
DB_DATABASE=esports_tracker

# Connection string (for worker)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_DATABASE}

# ==========================================
# REDIS
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# Connection string (for worker)
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379

# ==========================================
# APPLICATION
# ==========================================
NODE_ENV=production
APP_KEY=CHANGE_ME_RUN_openssl_rand_hex_32

# ==========================================
# RIOT GAMES API
# ==========================================
# Get your key at: https://developer.riotgames.com/
RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ==========================================
# FRONTEND
# ==========================================
NEXT_PUBLIC_API_URL=https://api.${DOMAIN}
NEXT_PUBLIC_WS_URL=wss://api.${DOMAIN}

# ==========================================
# GITHUB CONTAINER REGISTRY
# ==========================================
# Your GitHub username/org
GITHUB_REPOSITORY=your-username/esports-tracker

# ==========================================
# TRAEFIK (Reverse Proxy)
# ==========================================
# Generate with: htpasswd -nb admin YOUR_PASSWORD
# Or use: echo $(htpasswd -nb admin admin) | sed -e s/\\$/\\$\\$/g
TRAEFIK_AUTH=admin:$$apr1$$xxxxxxxx$$xxxxxxxxxxxxxxxxxxxxxxxx

# ==========================================
# MONITORING (Optional)
# ==========================================
# Email for alerts (requires mail setup)
# ALERT_EMAIL=alerts@esports-tracker.com

# Slack webhook for alerts
# SLACK_WEBHOOK=https://hooks.slack.com/services/xxx/xxx/xxx

# ==========================================
# BACKUP (Optional)
# ==========================================
# Number of days to keep backups
BACKUP_RETENTION_DAYS=7

# S3 bucket for remote backups (optional)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_BACKUP_BUCKET=esports-tracker-backups
